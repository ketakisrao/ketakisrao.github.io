RewriteEngine on
# Rewrite everything else to index.html to allow html5 state links
# Don't rewrite files or directories
#RewriteBase /
RewriteCond %{REQUEST_URI} ^/UIComponents [NC]
# redirect other to index.shtml
RewriteRule . UIComponents/index.html [L]